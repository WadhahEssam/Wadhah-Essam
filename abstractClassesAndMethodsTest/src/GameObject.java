
public  abstract class GameObject { // abstract classes exist to be extended they can't be instantiated 

	
	public  abstract void draw () ; // doesn't have {}  && this method should be in the extending class ( player and menu )
	
}
